configurar proxy:
git config --global http.proxy http://proxy01:3128

eliminar config:
git congif --unset http.proxy

listar config:
git config --list

**********************************************

git remote add origin
git branch -M main

git push -u origin main para ir de local a remoto

iniciar proxecto:
git init

añadir todo:
git add .

subir o estado actual do proxecto: como unha copia de seguridade
git commit -m

git status -s:

git log --oneline:

$ git remote add origin https://github.com/CarlosMilletFdez/Prueba_git2.git

$ git branch -M main:

$ git push -u origin main: para subir de local a remoto

cat + "nome documento": permite ver o contido do documento

git pull: para actualizar o documento de remoto a local

git tag

git push -tag 

git clone +  url para recuperar arquivos borrados en local dende o repositorio

ls: ver o contido da rama na que estamos

git branch "nombre" crear unha rama

git branch: ver as ramas

git checkout + "nombre rama": para moverse
